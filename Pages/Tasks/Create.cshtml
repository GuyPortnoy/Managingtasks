@page
@model Managingtasks.Pages.Tasks.CreateModel
@using Managingtasks.Models.Enums
@{
    ViewData["Title"] = "Create Task";
}

<div class="row justify-content-center">
    <div class="col-md-8 col-lg-6">
        <div class="glass-card mt-4">
            <div class="text-center mb-4">
                <span class="fs-1">📝</span>
                <h2 class="fw-bold mt-2">Create New Task</h2>
                <p class="text-muted small">Add a new task to your dashboard with custom priorities and status.</p>
            </div>

            <form method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger small mb-3"></div>

                <!-- Title Input -->
                <div class="premium-form-group">
                    <label asp-for="TaskItem.Title" class="premium-form-label">Task Title</label>
                    <input asp-for="TaskItem.Title" class="premium-input" placeholder="e.g. Design Landing Page" required minlength="2" />
                    <span asp-validation-for="TaskItem.Title" class="text-danger small mt-1 d-block"></span>
                </div>

                <!-- Description Textarea -->
                <div class="premium-form-group">
                    <label asp-for="TaskItem.Description" class="premium-form-label">Description</label>
                    <textarea asp-for="TaskItem.Description" class="premium-textarea" rows="4" placeholder="Briefly describe the task objectives..."></textarea>
                    <span asp-validation-for="TaskItem.Description" class="text-danger small mt-1 d-block"></span>
                </div>

                <div class="row">
                    <!-- Priority Selector -->
                    <div class="col-sm-6">
                        <div class="premium-form-group">
                            <label asp-for="TaskItem.Priority" class="premium-form-label">Priority</label>
                            <select asp-for="TaskItem.Priority" class="premium-select" asp-items="Html.GetEnumSelectList<TaskPriority>()">
                            </select>
                            <span asp-validation-for="TaskItem.Priority" class="text-danger small mt-1 d-block"></span>
                        </div>
                    </div>

                    <!-- Status Selector -->
                    <div class="col-sm-6">
                        <div class="premium-form-group">
                            <label asp-for="TaskItem.Status" class="premium-form-label">Initial Status</label>
                            <select asp-for="TaskItem.Status" class="premium-select" asp-items="Html.GetEnumSelectList<TaskItemStatus>()">
                            </select>
                            <span asp-validation-for="TaskItem.Status" class="text-danger small mt-1 d-block"></span>
                        </div>
                    </div>
                </div>

                <!-- Actions -->
                <div class="d-flex justify-content-between align-items-center mt-4">
                    <a asp-page="/Tasks/Index" class="text-light-hover small text-decoration-none">
                        ⬅ Back to Dashboard
                    </a>
                    <button type="submit" class="btn-premium pulse-hover">
                        💾 Save Task
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}